{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "uv sync backend",
      "type": "shell",
      "command": "uv sync",
      "options": {
        "cwd": "${workspaceFolder}/app/backend"
      }
    }
  ]
}
