/* eslint-disable no-console */
// eslint-disable-next-line @typescript-eslint/naming-convention
const log = {
  debug: console.log,
  error: console.error,
  warn: console.warn,
};

export default log;
