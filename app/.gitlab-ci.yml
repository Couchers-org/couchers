stages:
  - protos
  - build
  - test
  - preview
  - wait
  - release
  - deploy

variables:
  # whether to do checks or only build & deploy
  DO_CHECKS: "true"
  # whether to build web since it's very slow
  BUILD_WEB: "true"
  BUILD_MOBILE: "true"
  PREVIEW_DOMAIN: preview.couchershq.org
  # Branch on which a fingerprint change triggers an automatic Dev Tool native
  # rebuild (build:devtool-native). Set to mobile/v1.1.20 while validating the
  # machinery; switch to develop (i.e. $RELEASE_BRANCH) once it's trusted.
  DEVTOOL_BUILD_BRANCH: mobile/v1.1.20
  GIT_DEPTH: 1  # Speed up the checkout.
  DOCKER_HOST: tcp://docker:2376
  DOCKER_TLS_CERTDIR: "/certs"
  RELEASE_BRANCH: develop
  GRPC_TAG: registry.gitlab.com/couchers/grpc:latest
  PROXY_RELEASE_TAG: $CI_REGISTRY_IMAGE/proxy:latest
  NGINX_RELEASE_TAG: $CI_REGISTRY_IMAGE/nginx:latest
  NGINX_NEXT_RELEASE_TAG: $CI_REGISTRY_IMAGE/nginx-next:latest
  BACKEND_RELEASE_TAG: $CI_REGISTRY_IMAGE/backend:latest
  MEDIA_RELEASE_TAG: $CI_REGISTRY_IMAGE/media:latest
  PROMETHEUS_RELEASE_TAG: $CI_REGISTRY_IMAGE/prometheus:latest
  WEB_DEV_RELEASE_TAG: $CI_REGISTRY_IMAGE/web-dev:latest
  WEB_RELEASE_TAG: $CI_REGISTRY_IMAGE/web:latest
  WEB_NEXT_RELEASE_TAG: $CI_REGISTRY_IMAGE/web-next:latest
  SLUG: $CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  COMMIT_SHA: $CI_COMMIT_SHA
  COMMIT_TIMESTAMP: $CI_COMMIT_TIMESTAMP
  PROXY_TAG: $CI_REGISTRY_IMAGE/proxy:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  NGINX_TAG: $CI_REGISTRY_IMAGE/nginx:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  NGINX_NEXT_TAG: $CI_REGISTRY_IMAGE/nginx-next:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  BACKEND_TAG: $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  MEDIA_TAG: $CI_REGISTRY_IMAGE/media:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  WEB_TAG: $CI_REGISTRY_IMAGE/web:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  WEB_DEV_TAG: $CI_REGISTRY_IMAGE/web-dev:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  WEB_NEXT_TAG: $CI_REGISTRY_IMAGE/web-next:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  PROMETHEUS_TAG: $CI_REGISTRY_IMAGE/prometheus:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA

default:
  image: docker
  services:
    - docker:dind
  before_script: |
    # Error out when accessing an undefined variable
    set -u

    # All jobs that use this must have full git history to correctly set the version number.
    if [ "$GIT_DEPTH" == "0" ]; then
        export COMMIT_NUMBER=$(git rev-list --count "origin/$RELEASE_BRANCH")
        export DISPLAY_VERSION=$(base_ver=$(cat app/version); if [ "$CI_COMMIT_BRANCH" == "$RELEASE_BRANCH" ]; then echo "${base_ver}.${COMMIT_NUMBER}"; else echo "${base_ver}-$SLUG"; fi)
        # Debug version: {displayVersion}.{gitHash}.{gitCommitTime}, e.g.
        # v1.2.18410.1156180a.20260528Z0533. The git hash and commit time pin the
        # exact source the bundle was built from; both are stable per commit (not
        # build time) so the same source always yields the same DEBUG_VERSION.
        # CI_COMMIT_SHORT_SHA is GitLab's first-8-chars short SHA; the commit time
        # is formatted via git since busybox `date` can't reliably reparse the
        # offset-bearing CI_COMMIT_TIMESTAMP into UTC.
        export GIT_COMMIT_TIME=$(TZ=UTC git show -s --date=format-local:'%Y%m%dZ%H%M' --format=%cd HEAD)
        export DEBUG_VERSION="${DISPLAY_VERSION}.${CI_COMMIT_SHORT_SHA}.${GIT_COMMIT_TIME}"
        echo $DISPLAY_VERSION
        echo $DEBUG_VERSION
    fi

    docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY

# Reusable DISPLAY_VERSION / DEBUG_VERSION computation for jobs that opt out of
# the default before_script via `inherit: default: false` (mobile jobs skip
# docker login). Splice via
# `before_script: [!reference [.compute-display-version, before_script]]`
# and set `GIT_DEPTH: 0` so git rev-list has full history.
.compute-display-version:
  before_script:
    - |
      set -u
      export COMMIT_NUMBER=$(git rev-list --count "origin/$RELEASE_BRANCH")
      base_ver=$(cat app/version)
      if [ "$CI_COMMIT_BRANCH" = "$RELEASE_BRANCH" ]; then
        export DISPLAY_VERSION="${base_ver}.${COMMIT_NUMBER}"
      else
        export DISPLAY_VERSION="${base_ver}-${SLUG}"
      fi
      # Debug version: {displayVersion}.{gitHash}.{gitCommitTime}, e.g.
      # v1.2.18410.1156180a.20260528Z0533. Both components are stable per commit
      # (not build time), so the same source always yields the same DEBUG_VERSION.
      # CI_COMMIT_SHORT_SHA is GitLab's first-8-chars short SHA; the commit time is
      # formatted via git since busybox `date` can't reliably reparse the
      # offset-bearing CI_COMMIT_TIMESTAMP into UTC. The OTA bundle later appends
      # -{fingerprint}-{assetId}-{createdAt} at runtime (assetId/createdAt come
      # from the served manifest, which tools/ re-stamps on every publish — see
      # service/buildInfo.ts).
      export GIT_COMMIT_TIME=$(TZ=UTC git show -s --date=format-local:'%Y%m%dZ%H%M' --format=%cd HEAD)
      export DEBUG_VERSION="${DISPLAY_VERSION}.${CI_COMMIT_SHORT_SHA}.${GIT_COMMIT_TIME}"
      echo "DISPLAY_VERSION=$DISPLAY_VERSION"
      echo "DEBUG_VERSION=$DEBUG_VERSION"

.common_functions: &common_functions |
    # Build a Docker image using cache from latest "develop" build.
    # If this is a pipeline on "develop", push Docker build cache to the registry.
    # Usage:
    #   build "$BACKEND_TAG" "$BACKEND_RELEASE_TAG" "backend" "1.2.3"
    #   build "$BACKEND_TAG" "$BACKEND_RELEASE_TAG" "backend" "1.2.3" --arg  # extra args
    build() {
      local branch_tag="$1"
      local release_tag="$2"
      local path="$3"
      local version="$4"
      shift 4 # Remove the N arguments so that the rest can be used in "$@" later

      # A separate image with cached layers.
      cache_tag="${release_tag}-cache"

      # Push image cache only on develop.
      local cache_to=""
      if [ "$CI_COMMIT_BRANCH" = "$RELEASE_BRANCH" ]; then
        cache_to="--cache-to type=registry,ref=$cache_tag,mode=max"
      fi

      # Use buildx, as "docker build" does not work with registry cache.
      docker pull "$cache_tag" || true
      docker context create my-builder
      docker buildx create my-builder --driver docker-container --use

      docker buildx build \
        --push \
        -t "$branch_tag" \
        --build-arg display_version="$DISPLAY_VERSION" \
        --build-arg commit_sha="$COMMIT_SHA" \
        --build-arg commit_timestamp="$COMMIT_TIMESTAMP" \
        --build-arg version="$version" \
        --cache-from "$cache_tag" \
        ${cache_to} \
        "$@" \
        "app/$path/"
    }

protos:
  needs: []
  stage: protos
  image: $GRPC_TAG
  inherit:
    # the grpc container doesn't have docker, no need to login
    default: false
  script:
    - cd app && ./generate_protos.sh
  artifacts:
    paths:
      - app/

build:deploy-manifest:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0
  script:
    - sh app/deployment/build_manifest.sh app/backend/src/couchers/migrations/versions/ artifacts/deploy-info
    - mkdir -p artifacts/deploy-info/protos
    - cp app/proto/gen/descriptors.pb "artifacts/deploy-info/protos/${SLUG}.pb"
  artifacts:
    paths:
      - artifacts/deploy-info
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

build:proxy:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$PROXY_TAG" "$PROXY_RELEASE_TAG" "proxy" "$SLUG"
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH
    - if: $CI_COMMIT_BRANCH != $RELEASE_BRANCH
      changes:
        - app/proto/**/*
        - app/proxy/**/*
        - app/.gitlab-ci.yml

build:nginx:
  needs: []
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$NGINX_TAG" "$NGINX_RELEASE_TAG" "nginx" "$SLUG"
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

build:prometheus:
  needs: []
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$PROMETHEUS_TAG" "$PROMETHEUS_RELEASE_TAG" "prometheus" "$SLUG"
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

build:backend:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$BACKEND_TAG" "$BACKEND_RELEASE_TAG" "backend" "$SLUG"
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH
    - if: $CI_COMMIT_BRANCH != $RELEASE_BRANCH
      changes:
        - app/proto/**/*
        - app/backend/**/*
        - app/.gitlab-ci.yml

build:media:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$MEDIA_TAG" "$MEDIA_RELEASE_TAG" "media" "$SLUG"
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH
    - if: $CI_COMMIT_BRANCH != $RELEASE_BRANCH
      changes:
        - app/proto/**/*
        - app/media/**/*
        - app/.gitlab-ci.yml

build:web-dev:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$WEB_DEV_TAG" "$WEB_DEV_RELEASE_TAG" "web" "$SLUG" --build-arg environment=next -f app/web/Dockerfile
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") &&($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") &&($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/web/**/*
        - app/.gitlab-ci.yml
  tags:
      - saas-linux-large-amd64

build:web:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - - *common_functions
    # Add "--load" so that the image is saved locally for "docker create"
    - build "$WEB_TAG" "$WEB_RELEASE_TAG" "web" "$SLUG" --build-arg environment=production --build-arg pipeline_id=$CI_PIPELINE_ID -f app/web/Dockerfile.prod --load
    # creates a new docker container (docker create returns the container name), and copies the static folder to the host
    - mkdir -p artifacts && docker cp $(docker create $WEB_TAG):/app/.next/static/ static
    - ./app/web/generate_static_manifest.sh static > static/manifest.txt
    - cp static/manifest.txt static/manifest-$CI_PIPELINE_ID-$CI_COMMIT_SHA.txt
    - mkdir -p artifacts
    - cd static && tar czf ../artifacts/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz .
  rules:
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/web/**/*
        - app/.gitlab-ci.yml
  artifacts:
    paths:
      - artifacts/
  tags:
    - saas-linux-large-amd64

build:web-next:
  needs: ["protos"]
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    # Add "--load" so that the image is saved locally for "docker create"
    - build "$WEB_NEXT_TAG" "$WEB_NEXT_RELEASE_TAG" "web" "$SLUG-next" --build-arg environment=next --build-arg pipeline_id=$CI_PIPELINE_ID -f app/web/Dockerfile.prod --load
    # creates a new docker container (docker create returns the container name), and copies the static folder to the host
    - mkdir -p artifacts && docker cp $(docker create $WEB_NEXT_TAG):/app/.next/static/ static
    - ./app/web/generate_static_manifest.sh static > static/manifest.txt
    - cp static/manifest.txt static/manifest-$CI_PIPELINE_ID-$CI_COMMIT_SHA.txt
    - mkdir -p artifacts
    - cd static && tar czf ../artifacts/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz .
  rules:
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/web/**/*
        - app/.gitlab-ci.yml
  artifacts:
    paths:
      - artifacts/
  tags:
    - saas-linux-large-amd64

build:nginx-next:
  needs: []
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for "git rev-list --count" in default.before_script
  script:
    - *common_functions
    - build "$NGINX_NEXT_TAG" "$NGINX_RELEASE_TAG" "nginx" "$SLUG-next" --build-arg environment=preview
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH
    - if: $CI_COMMIT_BRANCH != $RELEASE_BRANCH
      changes:
        - app/proto/**/*
        - app/nginx/**/*
        - app/.gitlab-ci.yml

test:backend:
  needs: ["build:backend"]
  stage: test
  parallel: 3
  image: $BACKEND_TAG
  inherit:
    # no docker login
    default: false
  services:
    - name: postgis/postgis:18-3.6
      alias: postgres
      command: [
        "postgres",
        "-c", "bgwriter_lru_maxpages=0",
        "-c", "jit=off",
        "-c", "fsync=off",
        "-c", "full_page_writes=off",
        "-c", "synchronous_commit=off",
        "-c", "wal_level=minimal",
        "-c", "wal_keep_size=100MB",
        "-c", "max_wal_senders=0",
      ]
  variables:
    POSTGRES_PASSWORD: c765064a49d18a95
    DATABASE_CONNECTION_STRING: "postgresql://postgres:c765064a49d18a95@postgres/testdb"
    COVERAGE_FILE: /app/.coverage.$CI_NODE_INDEX
  script:
    - cd /app && pytest --junitxml=junit.xml --cov=src --cov-report= --splits $CI_NODE_TOTAL --group $CI_NODE_INDEX --durations-path src/tests/.test_durations --store-durations src
    # Move the stored durations to a node-specific file
    - mv src/tests/.test_durations /app/.test_durations.$CI_NODE_INDEX
  after_script:
    - cp /app/junit.xml $CI_PROJECT_DIR/junit-$CI_NODE_INDEX.xml
    - cp /app/.coverage.$CI_NODE_INDEX $CI_PROJECT_DIR/
    - cp /app/.test_durations.$CI_NODE_INDEX $CI_PROJECT_DIR/
    - mkdir -p $CI_PROJECT_DIR/artifacts/test_artifacts
    - cp /app/test_artifacts/* $CI_PROJECT_DIR/artifacts/test_artifacts/ 2>/dev/null || true
  artifacts:
    reports:
      junit: junit-$CI_NODE_INDEX.xml
    paths:
      - .coverage.$CI_NODE_INDEX
      - .test_durations.$CI_NODE_INDEX
      - artifacts/test_artifacts
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/backend/**/*
  tags:
      - saas-linux-medium-amd64

# This is a separate job because the migration test takes 2 minutes and this interferes
# with how pytest-split splits the tests between nodes.
test:backend-migrations:
  needs: ["build:backend"]
  stage: test
  image: $BACKEND_TAG
  inherit:
    # no docker login
    default: false
  services:
    - name: postgis/postgis:18-3.6
      alias: postgres
      command: [
        "postgres",
        "-c", "bgwriter_lru_maxpages=0",
        "-c", "jit=off",
        "-c", "fsync=off",
        "-c", "full_page_writes=off",
        "-c", "synchronous_commit=off",
        "-c", "wal_level=minimal",
        "-c", "wal_keep_size=100MB",
        "-c", "max_wal_senders=0",
      ]
  variables:
    POSTGRES_PASSWORD: c765064a49d18a95
    DATABASE_CONNECTION_STRING: "postgresql://postgres:c765064a49d18a95@postgres/testdb"
    TEST_SCHEMA_OUTPUT_DIR: /app/schema_dumps
    COVERAGE_FILE: /app/.coverage.migrations
  before_script:
    # install the latest postgres from their repos to get pg_dump compatible with later postgres
    - apt-get update -qy && apt-get install -qyy wget gpg
    - wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | gpg --dearmor > /usr/share/keyrings/postgresql-archive-keyring.gpg
    # "trixie" is taken from backend's dockerfile "python:3.13-slim-trixie"
    - echo "deb [signed-by=/usr/share/keyrings/postgresql-archive-keyring.gpg] http://apt.postgresql.org/pub/repos/apt trixie-pgdg main" > /etc/apt/sources.list.d/pgdg.list
    - apt-get -qy update
    - apt-get -qyy install postgresql-client
  script:
    - cd /app && pytest --junitxml=junit.xml --cov=src --cov-report= src/tests/test_db.py::test_migrations
  after_script:
    - cp /app/junit.xml $CI_PROJECT_DIR/junit-migrations.xml
    - cp /app/.coverage.migrations $CI_PROJECT_DIR/
    # Copy schema dumps
    - mkdir -p $CI_PROJECT_DIR/artifacts/schema_dumps
    - cp /app/schema_dumps/*.sql $CI_PROJECT_DIR/artifacts/schema_dumps/ 2>/dev/null || true
  artifacts:
    reports:
      junit: junit-migrations.xml
    paths:
      - .coverage.migrations
      - artifacts/schema_dumps
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/backend/**/*
  tags:
    - saas-linux-medium-amd64

test:backend-format:
  needs: ["build:backend"]
  stage: test
  image: $BACKEND_TAG
  inherit:
    # no docker login
    default: false
  script:
    - cd /app
    - ruff check .
    - ruff check --diff .
    - ruff format --diff .
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/backend/**/*

test:backend-mypy:
  needs: ["build:backend"]
  stage: test
  image: $BACKEND_TAG
  inherit:
    # no docker login
    default: false
  script:
    - cd /app
    - mypy src/couchers
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/backend/**/*

test:media:
  needs: ["build:media"]
  stage: test
  image: $MEDIA_TAG
  inherit:
    # no docker login
    default: false
  variables:
    MEDIA_SERVER_FROM_ENV: 0
  script:
    - cd /app && pytest src --junitxml=junit.xml
  after_script:
    - cp /app/junit.xml $CI_PROJECT_DIR/junit.xml
  artifacts:
    reports:
      junit: junit.xml
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/media/**/*

test:web:
  needs: ["build:web-dev"]
  stage: test
  parallel: 3
  image: $WEB_DEV_TAG
  inherit:
    # no docker login
    default: false
  variables:
    NEXT_PUBLIC_API_BASE_URL: "http://localhost:8888"
    TZ: "UTC"
  script:
    - cd /app && yarn jest --runInBand --coverage --coverageDirectory=coverage-$CI_NODE_INDEX --reporters="default" --reporters="jest-junit" --shard=$CI_NODE_INDEX/$CI_NODE_TOTAL
  after_script:
    - cp /app/junit.xml $CI_PROJECT_DIR/junit-$CI_NODE_INDEX.xml
    - mkdir -p $CI_PROJECT_DIR/coverage-$CI_NODE_INDEX && cp -a /app/coverage-$CI_NODE_INDEX/. $CI_PROJECT_DIR/coverage-$CI_NODE_INDEX/
  artifacts:
    reports:
      junit: junit-$CI_NODE_INDEX.xml
    paths:
      - coverage-$CI_NODE_INDEX
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/web/**/*
  tags:
    - saas-linux-large-amd64

test:web-prettier:
  needs: ["build:web-dev"]
  stage: test
  image: $WEB_DEV_TAG
  inherit:
    # no docker login
    default: false
  script:
    - cd /app
    - yarn prettier --check *
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/web/**/*

test:web-linting:
  needs: ["build:web-dev"]
  stage: test
  image: $WEB_DEV_TAG
  inherit:
    # no docker login
    default: false
  script:
    - cd /app
    - yarn lint --max-warnings=0
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/web/**/*

test:mobile:
  needs: ["protos"]
  stage: test
  image: node:22
  inherit:
    default: false
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npm test -- --ci --coverage
  after_script:
    - cp app/mobile/coverage/cobertura-coverage.xml $CI_PROJECT_DIR/ || true
  coverage: '/^All files[^|]*\|[^|]*\s+([\d\.]+)/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: cobertura-coverage.xml
  rules:
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/mobile/**/*

test:mobile-linting:
  needs: ["protos"]
  stage: test
  image: node:22
  inherit:
    default: false
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npm run lint
  rules:
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/mobile/**/*

test:mobile-typecheck:
  needs: ["protos"]
  stage: test
  image: node:22
  inherit:
    default: false
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npx tsc --noEmit
  rules:
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/mobile/**/*

test:mobile-expo-doctor:
  needs: ["protos"]
  stage: test
  image: node:22
  inherit:
    default: false
  allow_failure: true
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npx expo-doctor
  rules:
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/mobile/package.json
        - app/mobile/package-lock.json

test:proxy:
  needs: ["build:proxy"]
  stage: test
  script:
    - docker run --name envoy-config-test -d -p 8888:8888 -p 9901:9901 $PROXY_TAG
    - sleep 10
    - |
      if [ "$(docker inspect -f '{{.State.Running}}' envoy-config-test)" != "true" ]; then
        echo "Container failed to start. Envoy configuration might be invalid."
        docker logs envoy-config-test
        exit 1
      fi
    - docker logs envoy-config-test
  after_script:
    - docker rm -f envoy-config-test || true
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/proxy/**/*


preview:backend-coverage:
  needs:
    - job: test:backend
      artifacts: true
    - job: test:backend-migrations
      artifacts: true
  stage: preview
  image: python:3.14-slim
  inherit:
    # no docker login
    default: false
  script:
    # Install coverage
    - pip install coverage
    # Create coverage config to remap paths from Docker container to CI checkout
    - |
      cat > .coveragerc << EOF
      [paths]
      source =
          app/backend/src
          /app/src
      EOF
    # Combine coverage from all parallel workers (with path remapping)
    - coverage combine --rcfile=.coveragerc .coverage.*
    - coverage report --rcfile=.coveragerc
    - coverage xml --rcfile=.coveragerc
    - coverage html --rcfile=.coveragerc
    # Merge test durations from all parallel workers into a single test_durations.json.
    # This file can then be put into src/tests/.test_durations to update the way the
    # tests are split between the nodes by pytest-split. This only has to be done when
    # slow tests are added - unknown tests are assumed to have average run time.
    # Note that this has to be done on a CI machine, as test run time can be very different
    # from your local machine.
    - |
      python3 << 'EOF'
      import json
      import glob
      merged = {}
      for f in glob.glob('.test_durations.*'):
          with open(f) as fp:
              data = json.load(fp)
              merged.update(data)
      with open('test_durations.json', 'w') as fp:
          json.dump(merged, fp, sort_keys=True, indent=4)
      print(f"Merged {len(merged)} test durations from {len(glob.glob('.test_durations.*'))} files")
      EOF
  coverage: '/^TOTAL.+?(\d+\%)$/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
    paths:
      - htmlcov
      - test_durations.json
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/backend/**/*

preview:backend:
  needs:
    - job: test:backend
      artifacts: true
    - job: test:backend-migrations
      artifacts: true
    - job: preview:backend-coverage
      artifacts: true
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    # Create cleaned schema.sql with git hash header (only if schema file exists)
    - |
      if [ -f "artifacts/schema_dumps/schema_from_migrations.sql" ]; then
        {
          echo "-- Couchers.org database schema"
          echo "-- Generated from migrations at commit $CI_COMMIT_SHA on branch $CI_COMMIT_REF_NAME at $CI_COMMIT_TIMESTAMP"
          echo ""
          grep -v '^\\\restrict' artifacts/schema_dumps/schema_from_migrations.sql \
            | grep -v '^\\\unrestrict' \
            | grep -v '^--' \
            | grep -v 'ALTER .* OWNER TO postgres;' \
            | cat -s
        } > artifacts/schema_dumps/schema.sql
      fi
    # Upload coverage report to S3
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/bcov/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 cp htmlcov s3://$AWS_PREVIEW_BUCKET/bcov/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/bcov/$CI_COMMIT_REF_SLUG/ --recursive
    - aws s3 cp htmlcov s3://$AWS_PREVIEW_BUCKET/bcov/$CI_COMMIT_REF_SLUG/ --recursive
    - echo "Done, coverage report available at https://$CI_COMMIT_SHORT_SHA--bcov.$PREVIEW_DOMAIN/ and https://$CI_COMMIT_REF_SLUG--bcov.$PREVIEW_DOMAIN/"
    # Upload schema dumps
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/schema/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 cp artifacts/schema_dumps s3://$AWS_PREVIEW_BUCKET/schema/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/schema/$CI_COMMIT_REF_SLUG/ --recursive
    - aws s3 cp artifacts/schema_dumps s3://$AWS_PREVIEW_BUCKET/schema/$CI_COMMIT_REF_SLUG/ --recursive
    - echo "Done, schema dumps available at https://$CI_COMMIT_SHORT_SHA--schema.$PREVIEW_DOMAIN/schema.sql and https://$CI_COMMIT_REF_SLUG--schema.$PREVIEW_DOMAIN/schema.sql"
    # Upload postcard samples
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/test-artifacts/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 cp artifacts/test_artifacts s3://$AWS_PREVIEW_BUCKET/test-artifacts/$CI_COMMIT_SHORT_SHA/ --recursive 2>/dev/null || true
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/test-artifacts/$CI_COMMIT_REF_SLUG/ --recursive
    - aws s3 cp artifacts/test_artifacts s3://$AWS_PREVIEW_BUCKET/test-artifacts/$CI_COMMIT_REF_SLUG/ --recursive 2>/dev/null || true
    - echo "Done, test artifacts available at https://$CI_COMMIT_SHORT_SHA--test-artifacts.$PREVIEW_DOMAIN/ and https://$CI_COMMIT_REF_SLUG--test-artifacts.$PREVIEW_DOMAIN/"
  artifacts:
    paths:
      - artifacts/schema_dumps
      - artifacts/test_artifacts
  rules:
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/backend/**/*

preview:web-coverage:
  needs:
    - job: test:web
      artifacts: true
    - job: build:web-dev
      artifacts: false
  stage: preview
  image: $WEB_DEV_TAG
  inherit:
    # no docker login
    default: false
  script:
    - cd /app
    # Install nyc for merging coverage
    - npm install -g nyc
    # Merge coverage JSON files from all parallel workers
    - mkdir -p merged-coverage
    - nyc merge $CI_PROJECT_DIR/coverage-1 merged-coverage/coverage-1.json || true
    - nyc merge $CI_PROJECT_DIR/coverage-2 merged-coverage/coverage-2.json || true
    - nyc merge $CI_PROJECT_DIR/coverage-3 merged-coverage/coverage-3.json || true
    # Generate combined reports
    - nyc merge merged-coverage .nyc_output/coverage.json
    - nyc report --reporter=text --reporter=cobertura --reporter=lcov --report-dir=coverage
    # Move lcov-report to artifacts location
    - mkdir -p $CI_PROJECT_DIR/artifacts && mv coverage/lcov-report $CI_PROJECT_DIR/artifacts/
    - cp coverage/cobertura-coverage.xml $CI_PROJECT_DIR/
  coverage: '/^All files[^|]*\|[^|]*\s+([\d\.]+)/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: cobertura-coverage.xml
    paths:
      - artifacts/lcov-report
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/web/**/*

preview:wcov:
  needs: ["preview:web-coverage"]
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/wcov/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 cp artifacts/lcov-report s3://$AWS_PREVIEW_BUCKET/wcov/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 rm s3://$AWS_PREVIEW_BUCKET/wcov/$CI_COMMIT_REF_SLUG/ --recursive
    - aws s3 cp artifacts/lcov-report s3://$AWS_PREVIEW_BUCKET/wcov/$CI_COMMIT_REF_SLUG/ --recursive
    - echo "Done, coverage report available at https://$CI_COMMIT_SHORT_SHA--wcov.$PREVIEW_DOMAIN/ and https://$CI_COMMIT_REF_SLUG--wcov.$PREVIEW_DOMAIN/"
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/web/**/*

preview:web:
  needs: ["build:web"]
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - aws s3 cp artifacts s3://$AWS_PREVIEW_BUCKET/web-static/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 cp artifacts s3://$AWS_PREVIEW_BUCKET/web-static/$CI_COMMIT_REF_SLUG/ --recursive
    - echo "Done, web-static available at https://$CI_COMMIT_SHORT_SHA--web-static.$PREVIEW_DOMAIN/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz and https://$CI_COMMIT_REF_SLUG--web-static.$PREVIEW_DOMAIN/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz"
  rules:
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
      - app/web/**/*

preview:web-next:
  needs: ["build:web-next"]
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - aws s3 cp artifacts s3://$AWS_PREVIEW_BUCKET/web-next-static/$CI_COMMIT_SHORT_SHA/ --recursive
    - aws s3 cp artifacts s3://$AWS_PREVIEW_BUCKET/web-next-static/$CI_COMMIT_REF_SLUG/ --recursive
    - echo "Done, web-next-static available at https://$CI_COMMIT_SHORT_SHA--web-next-static.$PREVIEW_DOMAIN/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz and https://$CI_COMMIT_REF_SLUG--web-next-static.$PREVIEW_DOMAIN/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz"
  rules:
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
      - app/web/**/*

# Per-branch Dev Tool OTA preview: bundle the devtool variant and stage the Expo
# Updates manifest (see docs/mobile-dev-tool-ota.md), then publish to the existing
# dev-assets bucket under an immutable per-SHA prefix. The installed Dev Tool dev
# client loads it via couchers-devtool://expo-development-client/?url=...
build:mobile-ota:
  needs: ["protos"]
  stage: build
  image: node:22
  inherit:
    default: false
  variables:
    # Must build the devtool variant so the fingerprint matches the installed
    # Dev Tool dev client; a mismatch makes the client silently ignore the update.
    APP_VARIANT: devtool
    EXPO_PUBLIC_COUCHERS_ENV: preview
    EXPO_PUBLIC_API_BASE_URL: https://dev-api.couchershq.org
    EXPO_PUBLIC_WEB_BASE_URL: https://next.couchershq.org
    OTA_PLATFORMS: "ios android"
    GIT_DEPTH: 0  # full history so git rev-list can compute DISPLAY_VERSION
  before_script:
    - !reference [.compute-display-version, before_script]
  script:
    - cd app/mobile
    - npm ci
    # protos are generated by the `protos` job and pulled in via `needs`; copy the
    # grpc-web TS stubs in rather than re-downloading protoc and regenerating.
    - mkdir -p proto && cp -a ../proto/gen/ts/proto/. proto/
    - npx expo config --type public --json > ota-expo-config.json
    - |
      for P in $OTA_PLATFORMS; do
        rm -rf dist
        npx expo export --platform "$P"
        RV=$(npx expo-updates fingerprint:generate --platform "$P" 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>process.stdout.write(JSON.parse(s).hash))')
        echo "fingerprint($P) = $RV"
        node scripts/ota-stage.mjs --platform "$P" --runtime-version "$RV" --base-url "https://$CI_COMMIT_SHORT_SHA--ota.$PREVIEW_DOMAIN" --dist dist --out ota-out --expo-config ota-expo-config.json
        # QR encodes the dev-launcher deep link so the PR comment can show a scannable image.
        # npx (not a package.json dep) keeps it out of the fingerprint sources.
        npx --yes qrcode -o "ota-out/$P/qr.png" "couchers-devtool://expo-development-client/?url=https%3A%2F%2F$CI_COMMIT_SHORT_SHA--ota.$PREVIEW_DOMAIN%2F$P%2Fmanifest" < /dev/null
      done
  artifacts:
    paths:
      - app/mobile/ota-out/
    expire_in: 1 week
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/mobile/**/*
        - app/scripts/**/*

preview:mobile-ota:
  needs: ["build:mobile-ota"]
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  variables:
    OTA_PLATFORMS: "ios android"
  script:
    - |
      for P in $OTA_PLATFORMS; do
        D="app/mobile/ota-out/$P"
        CT="$(cat "$D/manifest.content-type")"
        # manifest is the protocol-v1 multipart body; its content-type carries the
        # boundary. The expo-protocol-version response header is injected by the
        # preview-origin-response Lambda@Edge (S3 can't set it).
        aws s3 cp "$D/manifest" "s3://$AWS_PREVIEW_BUCKET/ota/$CI_COMMIT_SHORT_SHA/$P/manifest" --content-type "$CT"
        aws s3 cp "$D/bundle.hbc" "s3://$AWS_PREVIEW_BUCKET/ota/$CI_COMMIT_SHORT_SHA/$P/bundle.hbc" --content-type application/javascript
        aws s3 cp "$D/assets/" "s3://$AWS_PREVIEW_BUCKET/ota/$CI_COMMIT_SHORT_SHA/$P/assets/" --recursive
        aws s3 cp "$D/qr.png" "s3://$AWS_PREVIEW_BUCKET/ota/$CI_COMMIT_SHORT_SHA/$P/qr.png" --content-type image/png
        aws s3 cp "$D/open.html" "s3://$AWS_PREVIEW_BUCKET/ota/$CI_COMMIT_SHORT_SHA/$P/open.html" --content-type "text/html; charset=utf-8"
        echo "Done $P. Manifest: https://$CI_COMMIT_SHORT_SHA--ota.$PREVIEW_DOMAIN/$P/manifest"
        echo "  deep link: couchers-devtool://expo-development-client/?url=https%3A%2F%2F$CI_COMMIT_SHORT_SHA--ota.$PREVIEW_DOMAIN%2F$P%2Fmanifest"
      done
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/mobile/**/*
        - app/scripts/**/*

# Aggregate per-PR preview links into one sticky GitHub PR comment. Runs after the
# upload jobs (needs) so every link/QR it posts is already live on CloudFront.
# Currently surfaces the mobile Dev Tool OTA preview; add more sections (web,
# coverage, ...) in app/scripts/pr_preview_comment.py as previews grow. Requires
# GITHUB_PREVIEW_TOKEN; no-ops when there's no open PR, so it never reds the pipeline.
preview:pr-comment:
  needs:
    - job: preview:mobile-ota
      artifacts: false
  stage: preview
  image: python:3.12-slim
  inherit:
    default: false
  variables:
    OTA_PLATFORMS: "ios android"
  script:
    - pip install --quiet --no-cache-dir requests
    - python app/scripts/pr_preview_comment.py
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH != $RELEASE_BRANCH)
      changes:
        - app/proto/**/*
        - app/mobile/**/*
        - app/scripts/**/*

# Rebuild the Dev Tool *native* client when its fingerprint changes — a native
# dep / app.config.js / Expo SDK change, the ~5% case OTA can't serve (a
# fingerprint mismatch makes the dev client silently ignore branch bundles; see
# docs/mobile-dev-tool-ota.md). scripts/devtool-build.sh builds on EAS:
# iOS -> TestFlight (auto-submit); Android -> a sideloadable APK published to
# s3://$AWS_PREVIEW_BUCKET/devtool-builds/android/ (Google Play has no
# TestFlight-style channel for a dev-client APK). It no-ops when the freshly
# computed fingerprint matches the last-built client, recorded per platform under
# s3://$AWS_PREVIEW_BUCKET/devtool-builds/. Requires the EXPO_TOKEN CI variable.
# Runs only on $DEVTOOL_BUILD_BRANCH while validating; point that at develop once
# trusted.
build:devtool-native:
  needs: ["protos"]
  stage: build
  image: node:22
  inherit:
    default: false
  variables:
    # Compute the fingerprint as the devtool variant so it matches the EAS build
    # (app.config.js branches on APP_VARIANT for bundle id / scheme / manifest).
    APP_VARIANT: devtool
  before_script:
    - apt-get update -qq && apt-get install -y --no-install-recommends awscli curl
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npm install -g eas-cli
    # Separate lines (not a loop) so an iOS failure fails the job instead of being
    # masked by a later Android success.
    - bash scripts/devtool-build.sh ios
    - bash scripts/devtool-build.sh android
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $DEVTOOL_BUILD_BRANCH)
      changes:
        - app/proto/**/*
        - app/mobile/**/*

# Staging OTA auto-deploy. Mirrors the web staging deploy (build a static bundle,
# publish to dev-assets, serve from there): bundle the staging variant, stage the
# (unsigned) Expo Updates output, upload bundle+assets to the dev-assets CDN under
# an immutable per-SHA prefix, and print the native_ota_bundles JSON. The staging
# backend (dev-api.couchershq.org/native/ota/manifest) builds the served manifest
# from that flag, with asset URLs pointing at what this job uploaded. Runs on
# develop only. Production publishing goes through the tools/ ops deploy.
build:mobile-ota-staging:
  needs: ["protos"]
  stage: build
  image: node:22
  inherit:
    default: false
  variables:
    APP_VARIANT: staging
    EXPO_PUBLIC_COUCHERS_ENV: preview
    EXPO_PUBLIC_API_BASE_URL: https://dev-api.couchershq.org
    EXPO_PUBLIC_WEB_BASE_URL: https://next.couchershq.org
    OTA_PLATFORMS: "ios android"
    GIT_DEPTH: 0  # full history so git rev-list can compute DISPLAY_VERSION
  before_script:
    - !reference [.compute-display-version, before_script]
  script:
    - cd app/mobile
    - npm ci
    # protos are generated by the `protos` job and pulled in via `needs`; copy the
    # grpc-web TS stubs in rather than re-downloading protoc and regenerating.
    - mkdir -p proto && cp -a ../proto/gen/ts/proto/. proto/
    - npx expo config --type public --json > ota-expo-config.json
    - |
      for P in $OTA_PLATFORMS; do
        rm -rf dist
        npx expo export --platform "$P" --source-maps
        # Upload this platform's JS source maps to Sentry before dist is recreated
        # next iteration. Symbolication matches on debug IDs embedded in the bundle
        # (getSentryExpoConfig in metro.config.js), so no release/dist coordination
        # is needed. Org/project come from the Expo config plugin; SENTRY_AUTH_TOKEN
        # is a required CI variable.
        npx sentry-expo-upload-sourcemaps dist
        RV=$(npx expo-updates fingerprint:generate --platform "$P" 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>process.stdout.write(JSON.parse(s).hash))')
        echo "fingerprint($P) = $RV"
        # The asset URLs baked into the manifest must resolve to the upload location
        # below; the CDN host maps <sha>--ota-staging -> the /ota-staging/<sha>/ prefix.
        node scripts/ota-stage.mjs --platform "$P" --runtime-version "$RV" --base-url "https://$CI_COMMIT_SHORT_SHA--ota-staging.$PREVIEW_DOMAIN" --dist dist --out ota-staging-out --expo-config ota-expo-config.json
      done
    - node scripts/ota-growthbook-flag.mjs --out ota-staging-out --platforms "$OTA_PLATFORMS" | tee ota-staging-out/native_ota_bundles.json
  artifacts:
    paths:
      - app/mobile/ota-staging-out/
    expire_in: 1 week
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

deploy:mobile-ota-staging:
  needs: ["build:mobile-ota-staging"]
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  variables:
    OTA_PLATFORMS: "ios android"
  script:
    - |
      for P in $OTA_PLATFORMS; do
        D="app/mobile/ota-staging-out/$P"
        # Only the bundle + assets go on the CDN; the manifest is served by the backend.
        aws s3 cp "$D/bundle.hbc" "s3://$AWS_PREVIEW_BUCKET/ota-staging/$CI_COMMIT_SHORT_SHA/$P/bundle.hbc" --content-type application/javascript
        if [ -d "$D/assets" ]; then
          aws s3 cp "$D/assets/" "s3://$AWS_PREVIEW_BUCKET/ota-staging/$CI_COMMIT_SHORT_SHA/$P/assets/" --recursive
        fi
      done
    - echo "Bundle + assets uploaded. Paste this into the staging GrowthBook 'native_ota_bundles' flag:"
    - cat app/mobile/ota-staging-out/native_ota_bundles.json
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

# Rebuild and submit the *production* store app when its fingerprint changes — the
# native counterpart to the OTA publish below. A signed OTA only applies to an
# installed build whose runtimeVersion (Expo fingerprint) matches, so a native dep
# / app.config.js / Expo SDK change (the ~5% case OTA can't serve) needs a fresh
# store build carrying the new fingerprint; pure JS/TS changes ship over the air.
#
# Split per platform and per phase: the `build` phase builds on EAS
# (scripts/production-build.sh, fingerprint-gated, emitting the build id to a
# dotenv artifact), and the `deploy` phase submits that build
# (scripts/production-submit.sh):
#   iOS     -> App Store Connect (lands in TestFlight + as an available build; it
#              is NOT submitted for App Store review and no release notes change —
#              do that by hand in App Store Connect).
#   Android -> the Play *internal* testing track (submit profile `production`,
#              track: internal); promote to public production by hand in the Play
#              Console.
# Neither path makes anything public on its own. The build no-ops (and the submit
# in turn) when the freshly computed fingerprint matches the last-submitted app,
# recorded per platform under s3://$AWS_PREVIEW_BUCKET/production-builds/. Requires
# the EXPO_TOKEN CI variable.
.production-native:
  image: node:22
  inherit:
    default: false
  before_script:
    - apt-get update -qq && apt-get install -y --no-install-recommends awscli curl
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

build:production-native-ios:
  extends: .production-native
  needs: ["protos"]
  stage: build
  variables:
    # Compute the fingerprint as the production variant so it matches the EAS build
    # (app.config.js branches on APP_VARIANT) and the OTA publish job's fingerprint.
    APP_VARIANT: production
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npm install -g eas-cli
    - bash scripts/production-build.sh ios
  artifacts:
    reports:
      dotenv: app/mobile/native-build-ios.env

build:production-native-android:
  extends: .production-native
  needs: ["protos"]
  stage: build
  variables:
    APP_VARIANT: production
  script:
    - cd app/mobile
    - npm ci
    - npm run build:protos
    - npm install -g eas-cli
    - bash scripts/production-build.sh android
  artifacts:
    reports:
      dotenv: app/mobile/native-build-android.env
  # Override .production-native's TEMP rule: the Android submit is disabled on the
  # temp branch (deploy:production-native-android is release-only), so there's no
  # marker write and this would otherwise rebuild on every pipeline. Build Android
  # only on the release branch, where build+submit+marker work end to end. Drop
  # this override when the TEMP rules are reverted (see TEMP-MOBILE.md).
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)

deploy:production-native-ios:
  extends: .production-native
  needs: ["build:production-native-ios"]
  stage: deploy
  script:
    - cd app/mobile
    - npm ci
    - npm install -g eas-cli
    - bash scripts/production-submit.sh ios
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: submit to TestFlight from this branch to test the OTA on-device handshake;
    # revert before merge so submits only happen on the release branch.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

deploy:production-native-android:
  extends: .production-native
  needs: ["build:production-native-android"]
  stage: deploy
  script:
    - cd app/mobile
    - npm ci
    - npm install -g eas-cli
    - bash scripts/production-submit.sh android
  # Submit only on the real release branch — disabled on the temp validation branch
  # so we build (above) without pushing anything to the stores yet.
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)

# ─── Production native OTA publish (decoupled from the backend/web deploy) ───
#
# Mirrors the web static release flow (build a bundle -> upload a tarball to the
# preview CDN -> record a manifest in the deploy bucket), but on its OWN manifest
# stream (deploy/native-ota-manifests/) and WITHOUT depending on any release:*
# job, so a JS-only over-the-air update ships without a backend/web deploy.
#
# CI only stages UNSIGNED bundles. Signing and the push to the prod CDN happen
# later, in the tools/ deploy lambda's publish_native_ota action — the private
# signing key never touches CI. The manifest's asset URLs are baked with the
# literal placeholder __OTA_BUNDLE_BASE__; the publish step substitutes the real
# prod CDN base (<cdn-root>/<ota-version>, e.g. v1.2.18355.fc38c23d) before
# signing, so it alone owns where the bundle lives and the URLs can't drift from
# the upload path.
build:mobile-ota-prod:
  needs: ["protos"]
  stage: build
  image: node:22
  inherit:
    default: false
  variables:
    APP_VARIANT: production
    EXPO_PUBLIC_COUCHERS_ENV: prod
    EXPO_PUBLIC_API_BASE_URL: https://api.couchers.org
    EXPO_PUBLIC_WEB_BASE_URL: https://couchers.org
    OTA_PLATFORMS: "ios android"
    GIT_DEPTH: 0  # full history so git rev-list can compute DISPLAY_VERSION
  before_script:
    - !reference [.compute-display-version, before_script]
  script:
    - cd app/mobile
    - npm ci
    # protos are generated by the `protos` job and pulled in via `needs`; copy the
    # grpc-web TS stubs in rather than re-downloading protoc and regenerating.
    - mkdir -p proto && cp -a ../proto/gen/ts/proto/. proto/
    - npx expo config --type public --json > ota-expo-config.json
    - mkdir -p native-ota-dist
    - |
      for P in $OTA_PLATFORMS; do
        rm -rf dist
        npx expo export --platform "$P" --source-maps
        # Upload this platform's JS source maps to Sentry before dist is recreated
        # next iteration. Symbolication matches on debug IDs embedded in the bundle
        # (getSentryExpoConfig in metro.config.js), so no release/dist coordination
        # is needed. Org/project come from the Expo config plugin; SENTRY_AUTH_TOKEN
        # is a required CI variable.
        npx sentry-expo-upload-sourcemaps dist
        RV=$(npx expo-updates fingerprint:generate --platform "$P" 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>process.stdout.write(JSON.parse(s).hash))')
        echo "fingerprint($P) = $RV"
        # Asset URLs are baked with the __OTA_BUNDLE_BASE__ placeholder; the publish
        # lambda replaces it with the real prod CDN base before signing.
        node scripts/ota-stage.mjs --platform "$P" --runtime-version "$RV" --base-url '__OTA_BUNDLE_BASE__' --dist dist --out ota-prod-out --expo-config ota-expo-config.json
        # Tar the staged per-platform output (manifest, manifest.content-type,
        # bundle.hbc, assets/) so the publish lambda pulls one immutable artifact.
        tar -C ota-prod-out/"$P" -czf "native-ota-dist/native-ota-$P-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz" .
      done
  artifacts:
    paths:
      - app/mobile/native-ota-dist/
    expire_in: 1 week
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

preview:mobile-ota-prod:
  needs: ["build:mobile-ota-prod"]
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - aws s3 cp app/mobile/native-ota-dist/ s3://$AWS_PREVIEW_BUCKET/native-ota/$CI_COMMIT_SHORT_SHA/ --recursive
    - echo "Native OTA bundles staged at https://$CI_COMMIT_SHORT_SHA--native-ota.$PREVIEW_DOMAIN/"
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

# Own manifest stream, built and uploaded independently of build:deploy-manifest /
# deploy:deploy-info so a native OTA is never gated on a backend/web release.
build:native-ota-manifest:
  needs: []
  stage: build
  variables:
    GIT_DEPTH: 0  # needed for COMMIT_NUMBER / DISPLAY_VERSION in default before_script
  script:
    - sh app/deployment/build_native_ota_manifest.sh artifacts/native-ota-info
  artifacts:
    paths:
      - artifacts/native-ota-info
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

deploy:native-ota-info:
  needs:
    - job: build:native-ota-manifest
    - job: preview:mobile-ota-prod
      artifacts: false
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - aws s3 cp artifacts/native-ota-info/ "s3://$AWS_DEPLOY_BUCKET/" --recursive
  rules:
    - if: ($BUILD_MOBILE == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)
    # TEMP: run on this branch to validate the pipeline; revert before merge.
    - if: $CI_COMMIT_BRANCH == "mobile/v1.1.20"

preview:protos:
  needs: ["protos"]
  stage: preview
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - python_sum=$(sha256sum app/proto/gen/python.tar.gz | head -c 64)
    - ts_sum=$(sha256sum app/proto/gen/ts.tar.gz | head -c 64)
    - pb_sum=$(sha256sum app/proto/gen/descriptors.pb | head -c 64)
    - aws s3 cp app/proto/gen/python.tar.gz s3://$AWS_PREVIEW_BUCKET/by-sha/protos/$python_sum/python.tar.gz
    - aws s3 cp app/proto/gen/ts.tar.gz s3://$AWS_PREVIEW_BUCKET/by-sha/protos/$ts_sum/ts.tar.gz
    - aws s3 cp app/proto/gen/descriptors.pb s3://$AWS_PREVIEW_BUCKET/by-sha/protos/$pb_sum/descriptors.pb
    - aws s3api put-object --bucket $AWS_PREVIEW_BUCKET --key protos/$CI_COMMIT_REF_SLUG/ts.tar.gz --website-redirect-location https://protos--by-sha.$PREVIEW_DOMAIN/$ts_sum/ts.tar.gz
    - aws s3api put-object --bucket $AWS_PREVIEW_BUCKET --key protos/$CI_COMMIT_REF_SLUG/python.tar.gz --website-redirect-location https://protos--by-sha.$PREVIEW_DOMAIN/$python_sum/python.tar.gz
    - aws s3api put-object --bucket $AWS_PREVIEW_BUCKET --key protos/$CI_COMMIT_REF_SLUG/descriptors.pb --website-redirect-location https://protos--by-sha.$PREVIEW_DOMAIN/$pb_sum/descriptors.pb
    - >
      echo '<!DOCTYPE html><html><head><meta charset="utf-8"><style>body{font-family:monospace}</style></head><body>'
      '<h1>Couchers generated protos @ <a href="https://github.com/Couchers-org/couchers/commit/'$CI_COMMIT_SHA'">'$CI_COMMIT_SHORT_SHA'</a>.</h1>'
      '<p>Generated at '$(TZ=UTC date)'.</p>'
      '<ul>'
      '<li><a href="https://protos--by-sha.'$PREVIEW_DOMAIN'/'$python_sum'/python.tar.gz">Python (sha256: '$python_sum')</a></li>'
      '<li><a href="https://protos--by-sha.'$PREVIEW_DOMAIN'/'$ts_sum'/ts.tar.gz">TypeScript (sha256: '$ts_sum')</a></li>'
      '<li><a href="https://protos--by-sha.'$PREVIEW_DOMAIN'/'$pb_sum'/descriptors.pb">Descriptors (sha256: '$pb_sum')</a></li>'
      '</ul>'
      '<p>Redirects to latest protos on slug '$CI_COMMIT_REF_SLUG':</p>'
      '<ul>'
      '<li><a href="https://'$CI_COMMIT_REF_SLUG'--protos.'$PREVIEW_DOMAIN'/python.tar.gz">Python</a></li>'
      '<li><a href="https://'$CI_COMMIT_REF_SLUG'--protos.'$PREVIEW_DOMAIN'/ts.tar.gz">TypeScript</a></li>'
      '<li><a href="https://'$CI_COMMIT_REF_SLUG'--protos.'$PREVIEW_DOMAIN'/descriptors.pb">Descriptors</a></li>'
      '</ul>'
      '</body></html>' > app/proto/gen/index.html
    - aws s3 cp app/proto/gen/index.html s3://$AWS_PREVIEW_BUCKET/protos/$CI_COMMIT_SHORT_SHA/
    - aws s3 cp app/proto/gen/index.html s3://$AWS_PREVIEW_BUCKET/protos/$CI_COMMIT_REF_SLUG/
    - echo "Done, protos available at https://$CI_COMMIT_SHORT_SHA--protos.$PREVIEW_DOMAIN/ and https://$CI_COMMIT_REF_SLUG--protos.$PREVIEW_DOMAIN/"

# Having this here frees us from having to list out all the needs in each release below.
# List out all that need to be waited for until releasing a new version
wait:before-release:
  stage: wait
  inherit:
    # no docker login
    default: false
  script:
    - echo "Done waiting."
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH
  needs:
    - job: protos
      artifacts: false
    - job: build:proxy
      artifacts: false
    - job: build:nginx
      artifacts: false
    - job: build:prometheus
      artifacts: false
    - job: build:backend
      artifacts: false
    - job: build:media
      artifacts: false
    - job: build:web-dev
      artifacts: false
      optional: true
    - job: build:web
      artifacts: false
      optional: true
    - job: build:web-next
      artifacts: false
      optional: true
    - job: build:nginx-next
      artifacts: false
    - job: build:deploy-manifest
      artifacts: false
    - job: test:backend
      artifacts: false
      optional: true
    - job: test:backend-migrations
      artifacts: false
      optional: true
    - job: preview:backend
      artifacts: false
      optional: true
    - job: test:backend-format
      artifacts: false
      optional: true
    - job: test:backend-mypy
      artifacts: false
      optional: true
    - job: test:media
      artifacts: false
      optional: true
    - job: test:web
      artifacts: false
      optional: true
    - job: preview:web-coverage
      artifacts: false
      optional: true
    - job: test:web-prettier
      artifacts: false
      optional: true
    - job: test:web-linting
      artifacts: false
      optional: true
    - job: test:mobile
      artifacts: false
      optional: true
    - job: test:mobile-linting
      artifacts: false
      optional: true
    - job: test:mobile-typecheck
      artifacts: false
      optional: true

release:proxy:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $PROXY_TAG
    - docker tag $PROXY_TAG $PROXY_RELEASE_TAG
    - docker push $PROXY_RELEASE_TAG
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

release:nginx:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $NGINX_TAG
    - docker tag $NGINX_TAG $NGINX_RELEASE_TAG
    - docker push $NGINX_RELEASE_TAG
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

release:prometheus:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $PROMETHEUS_TAG
    - docker tag $PROMETHEUS_TAG $PROMETHEUS_RELEASE_TAG
    - docker push $PROMETHEUS_RELEASE_TAG
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

release:backend:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $BACKEND_TAG
    - docker tag $BACKEND_TAG $BACKEND_RELEASE_TAG
    - docker push $BACKEND_RELEASE_TAG
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

release:media:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $MEDIA_TAG
    - docker tag $MEDIA_TAG $MEDIA_RELEASE_TAG
    - docker push $MEDIA_RELEASE_TAG
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

release:web-dev:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $WEB_DEV_TAG
    - docker tag $WEB_DEV_TAG $WEB_DEV_RELEASE_TAG
    - docker push $WEB_DEV_RELEASE_TAG
  rules:
    - if: ($BUILD_WEB == "true") && ($DO_CHECKS == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)

release:web:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $WEB_TAG
    - docker tag $WEB_TAG $WEB_RELEASE_TAG
    - docker push $WEB_RELEASE_TAG
  rules:
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)

release:web-next:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $WEB_NEXT_TAG
    - docker tag $WEB_NEXT_TAG $WEB_NEXT_RELEASE_TAG
    - docker push $WEB_NEXT_RELEASE_TAG
    # some command to get our preview server to redeploy?
  rules:
    - if: ($BUILD_WEB == "true") && ($CI_COMMIT_BRANCH == $RELEASE_BRANCH)

release:nginx-next:
  needs: ["wait:before-release"]
  stage: release
  script:
    - docker pull $NGINX_NEXT_TAG
    - docker tag $NGINX_NEXT_TAG $NGINX_NEXT_RELEASE_TAG
    - docker push $NGINX_NEXT_RELEASE_TAG
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

deploy:deploy-info:
  needs:
    - job: build:deploy-manifest
    - job: release:proxy
      artifacts: false
    - job: release:nginx
      artifacts: false
    - job: release:prometheus
      artifacts: false
    - job: release:backend
      artifacts: false
    - job: release:media
      artifacts: false
    - job: release:web
      artifacts: false
      optional: true
    - job: release:web-next
      artifacts: false
      optional: true
    - job: release:nginx-next
      artifacts: false
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - aws s3 cp artifacts/deploy-info/ "s3://$AWS_DEPLOY_BUCKET/" --recursive
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH

deploy:staging:
  needs: ["release:web-next", "release:backend"]
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  inherit:
    # no docker login
    default: false
  script:
    - apt-get update && apt install -y git wget openssh-client
    - export rev=$(git rev-parse @ | cut -c-8)
    # Add the SSH key (https://docs.gitlab.com/ci/jobs/ssh_keys)
    - eval $(ssh-agent -s)
    - chmod 400 "$SSH_KEY"
    - ssh-add "$SSH_KEY"
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - cp "$SSH_KNOWN_HOSTS" ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
    # Deploy
    - echo "Git hash is ${rev}"
    - echo "Deploying staging..."
    - web_next_static="https://${rev}--web-next-static.preview.couchershq.org/static-$CI_PIPELINE_ID-$CI_COMMIT_SHA.tar.gz"
    - wget -q ${web_next_static} -O next-static.tar.gz
    - mkdir -p next/
    - tar xf next-static.tar.gz --directory next/
    - aws s3 sync next/ s3://next-static.couchershq.org/static/_next/static/
    - ssh ubuntu@next.couchershq.org "cd ~/couchers/app/deployment && ./install.sh && ./post-upgrade.sh"
    - echo "Done deploying staging, checking https://next.couchershq.org"
    # Check that it's working
    - ${CI_PROJECT_DIR}/app/deployment/check_deployment.sh develop-${rev} https://next.couchershq.org
  rules:
    - if: $CI_COMMIT_BRANCH == $RELEASE_BRANCH
