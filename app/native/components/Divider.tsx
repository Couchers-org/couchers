import { View } from "react-native";

export default function Divider() {
  return (
    <View
      style={{
        borderBottomColor: "gray",
        borderBottomWidth: 1,
        marginVertical: 16,
      }}
    />
  );
}
