import { makeStyles } from "@material-ui/core";
import React from "react";

const useStyles = makeStyles({
  root: {},
});

export default function HostingPreferenceForm() {
  return <div>Hosting preference form</div>;
}
