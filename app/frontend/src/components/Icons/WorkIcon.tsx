import React from "react";
import MuiWorkIcon from "@material-ui/icons/WorkOutline";
import { IconProps } from ".";

export function WorkIcon({ titleAccess }: IconProps) {
  return <MuiWorkIcon titleAccess={titleAccess} />;
}
