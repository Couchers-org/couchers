{
  "mcpServers": {
    "playwright": {
      "command": "bash",
      "args": [
        "-c",
        "command -v npx >/dev/null || . \"${NVM_DIR:-$HOME/.nvm}/nvm.sh\"; exec npx -y @playwright/mcp@latest --isolated --viewport-size=1280,900"
      ]
    }
  }
}
