deployment:
  ingress:
    hosts:
      - host: api.couchers.dev
        paths:
          - /
  envFrom:
    - secretRef:
        name: master-backend-dev-env
  extraEnvs:
    - name: "DATABASE_CONNECTION_STRING"
      valueFrom:
        secretKeyRef:
          name: backend-db
          key: database_uri 
