deployment:
  ingress:
    hosts:
      - host: api.staging.couchers.dev
        paths:
          - /
  envFrom:
    - secretRef:
        name: backend-env
  extraEnvs:
    - name: "DATABASE_CONNECTION_STRING"
      valueFrom:
        secretKeyRef:
          name: backend-db
          key: database_uri
